# Agent instructions

This project's rules live in **`CLAUDE.md`** at the repository root, with detail in `docs/`.

Read `CLAUDE.md` first. It is tool-agnostic despite the name — every rule in it applies regardless
of which coding agent is running.
